<!DOCTYPE html>
<html lang="ko">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>inhan blog</title>
    </head>
    <body>
        <h1><a href="./">inhan blog</a></h1>

        <ul style="display: flex; gap: 10px; list-style-type: none;">
            <li><a href="all/page/1">전체</a></li>
            {{#menuList}}
                <li><a href="{{id}}/page/1">{{name}}</a></li>
            {{/menuList}}
        </ul>

        <ul id="container" style="list-style-type: none;">
            {{#posts}}
            <li style="display:flex; gap:10px; margin:5px 0;">
                <div>
                    <a href="{{url}}">{{title}}</a>
                </div>
                <div>{{menuName}}</div>
                <div>{{dateString}}</div>
            </li>
            {{/posts}}
            {{^posts}}
                <li>글이 존재하지 않습니다</li>
            {{/posts}}
        </ul>

        {{#usePaging}}
        <ul id="pagination" style="display: flex; gap: 10px; list-style-type: none;">
            {{#pageNumbers}}
                {{#active}}
                    <li>{{num}}</li>
                {{/active}}
                {{^active}}
                    <li><a href="all/page/{{num}}">{{num}}</a></li>
                {{/active}}
            {{/pageNumbers}}
        </ul>
        {{/usePaging}}
    </body>
</html>
